# 14 — Production spin-up: one cloud host, on demand

Depends on: 09, 11, 13. Runs *after* the fleet track's registration work: by the time
this roadmap starts, hosts join the plane by registering, and this roadmap simply
runs that same path on a machine that isn't your laptop.

## Summary

Deploy the already-multi-host-capable plane to one cloud host, as a repeatable
capability rather than a standing milestone: one Hetzner Cloud host running the
control plane and `quickspin-hostd` (which registers with the plane exactly as the
Lima hostds do — no static-config prod fork exists to build), state in Neon, TLS on
a real domain (`api.quickspin.<yourdomain>`), managed by systemd, deployed by a
script in the repo — and torn down by one, so a full spin-up → accept → tear-down
cycle costs hours of billing, not a monthly bill. The CLI gains a base-URL knob so
the same commands you run against Lima drive prod from your Mac — the manual
acceptance pass this roadmap exists to enable. Local Lima remains the dev
environment; prod is a mode you can enter any time, not a gate the roadmap passes
through once.

<KeyPoint label="The Lima-first invariant">
  Every roadmap's definition of done validates in Lima; the cloud host runs the
  identical registration and deploy path, so nothing later forks on "local vs
  prod". Nothing in the plane's code becomes "prod-only" — the same binaries run in
  Lima and on Hetzner; only the environment differs. If a behavior needs an
  `if prod` branch, something upstream is mis-designed, and if the spin-up script
  stops working against the current tree, a roadmap has broken the shared path —
  that is a bug, not drift to accept.
</KeyPoint>

## Industry context

This is a platform's minimum viable production shape, and it is deliberately boring:
binary + systemd + Caddy + managed Postgres is how many real single-team services run
before Kubernetes is justified. Hetzner Cloud is the provider because it is cheap
with a simple API — and, as a free option, its ordinary VMs expose `/dev/kvm`, which
most cloud VMs don't, so the optional microVM track (roadmaps 26/19) needs no
re-provisioning if it is ever picked up. The core arc's isolation upgrade (gVisor,
roadmap 18) needs no KVM at all, so nothing here hangs on that. Everything
else (TLS termination in front of the plane, secrets
in an env file with tight permissions, deploy-by-artifact) is standard practice scaled
down to one machine.

## Key concepts

- Linux server operation for real: systemd units (`Restart=`, `After=`, journal
  logging), ufw firewalling (443 + SSH only — guest ports and the plane's plaintext
  port must not be public), unattended-upgrades.
- TLS and DNS in practice: an A record, Caddy as reverse proxy with automatic
  Let's Encrypt, and why the plane itself stays on localhost.
- Release engineering in miniature: `make release` (cross-compile linux/amd64 — note
  the arch flip from your arm64 Mac), `hack/deploy.sh` (rsync artifact, migrate via
  startup, restart unit, health-check, roll back on failure).
- Environment/config separation: the same binary, different env — `QUICKSPIN_DB`,
  backend default, base URL — twelve-factor style.

### Concept: the process tree belongs to systemd, not to you

In Lima you start the plane by hand and it dies when your terminal does. On a real
host something must answer three questions your shell never had to: who starts the
process at boot, who restarts it when it crashes, and where does its output go when
nobody is watching? systemd is Linux's answer to all three. A unit file declares the
answers as configuration — `ExecStart=` for what to run, `Restart=on-failure` for
crash recovery, `After=network-online.target` for ordering, `EnvironmentFile=` for
secrets — and the journal captures stdout/stderr with timestamps, queryable later via
`journalctl -u quickspin`. The failure mode that makes this matter is the reboot you
didn't schedule: Hetzner migrates a hypervisor, the host comes back, and either your
API is serving again within a minute with no human involved, or it stays down until
you happen to look. The reboot-resilience check in the definition of done exists to
prove which world you're in.

### Concept: TLS terminates at the edge; the plane never learns HTTPS

The plane keeps listening on plaintext `localhost:8080` forever. Caddy binds `:443`,
holds the certificate, and proxies decrypted requests over loopback. This split is the
industry default for a reason: certificate acquisition and renewal (ACME challenges
against Let's Encrypt, 90-day rotation) is a full protocol you should not embed in an
application binary, and Caddy does the whole thing automatically from a two-line
Caddyfile once DNS resolves. The corollary is a firewall obligation: the plaintext
port is only safe because it is unreachable. `ufw` must allow 443 and SSH and nothing
else — if 8080 is ever exposed, every request bypasses TLS *and*, depending on how
auth middleware is mounted, possibly your logging and rate paths too. The negative
probe in the validation script (assert connection refused on 8080 and a guest port
from the internet) is the regression test for this boundary.

<Figure caption="One host, three trust zones: the internet reaches only Caddy on 443; the plane and sandboxes live behind the firewall; Neon is the only external dependency.">
  <svg viewBox="0 0 620 250" role="img" aria-label="Single-host production topology: Mac client through Caddy to the plane and docker sandboxes, with Neon external">
    <defs>
      <marker id="arrow16" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#cf5436" />
      </marker>
    </defs>
    <rect x="10" y="88" width="110" height="44" fill="none" stroke="#18231f" strokeWidth="1.5" />
    <text x="65" y="108" textAnchor="middle" fontSize="12" fill="#18231f">Mac / CLI</text>
    <text x="65" y="123" textAnchor="middle" fontSize="10" fill="#405049">quickspin CLI + curl</text>
    <rect x="180" y="20" width="430" height="180" fill="none" stroke="#405049" strokeWidth="1.5" strokeDasharray="6 4" />
    <text x="395" y="38" textAnchor="middle" fontSize="10" fill="#405049">Hetzner host — ufw allows 443 + SSH only</text>
    <rect x="200" y="66" width="110" height="44" fill="none" stroke="#18231f" strokeWidth="1.5" />
    <text x="255" y="86" textAnchor="middle" fontSize="12" fill="#18231f">Caddy :443</text>
    <text x="255" y="101" textAnchor="middle" fontSize="10" fill="#405049">TLS, Let's Encrypt</text>
    <rect x="370" y="66" width="130" height="44" fill="none" stroke="#18231f" strokeWidth="1.5" />
    <text x="435" y="86" textAnchor="middle" fontSize="12" fill="#18231f">quickspin serve</text>
    <text x="435" y="101" textAnchor="middle" fontSize="10" fill="#405049">localhost:8080</text>
    <rect x="370" y="140" width="130" height="44" fill="none" stroke="#18231f" strokeWidth="1.5" />
    <text x="435" y="160" textAnchor="middle" fontSize="12" fill="#18231f">docker sandboxes</text>
    <text x="435" y="175" textAnchor="middle" fontSize="10" fill="#405049">via registered hostd</text>
    <line x1="120" y1="88" x2="198" y2="88" stroke="#cf5436" strokeWidth="1.5" markerEnd="url(#arrow16)" />
    <text x="159" y="78" textAnchor="middle" fontSize="10" fill="#8f3423">https://api...</text>
    <line x1="310" y1="88" x2="368" y2="88" stroke="#cf5436" strokeWidth="1.5" markerEnd="url(#arrow16)" />
    <text x="339" y="78" textAnchor="middle" fontSize="10" fill="#8f3423">plaintext</text>
    <line x1="435" y1="110" x2="435" y2="138" stroke="#cf5436" strokeWidth="1.5" markerEnd="url(#arrow16)" />
    <rect x="530" y="140" width="70" height="44" fill="none" stroke="#caa552" strokeWidth="1.5" />
    <text x="565" y="165" textAnchor="middle" fontSize="12" fill="#18231f">systemd</text>
    <path d="M 530 158 L 505 158" fill="none" stroke="#caa552" strokeWidth="1.5" strokeDasharray="5 4" />
    <path d="M 565 140 C 565 88 510 88 502 88" fill="none" stroke="#caa552" strokeWidth="1.5" strokeDasharray="5 4" />
    <rect x="250" y="216" width="110" height="28" fill="none" stroke="#caa552" strokeWidth="1.5" strokeDasharray="5 4" />
    <text x="305" y="234" textAnchor="middle" fontSize="11" fill="#18231f">Neon Postgres</text>
    <path d="M 400 110 C 390 170 360 212 362 214" fill="none" stroke="#caa552" strokeWidth="1.5" strokeDasharray="5 4" />
    <text x="150" y="232" textAnchor="middle" fontSize="10" fill="#405049">external, managed</text>
  </svg>
</Figure>

### Concept: a deploy is a transaction with a rollback branch

Copying a binary and restarting a unit is easy; doing it so a broken build cannot
take the API down is the actual skill. `hack/deploy.sh` should treat the deploy as
a sequence with a decision point: keep the previous binary aside, install the new
one, restart the unit, then *gate* on a health check (`/v1/status` returning the new
version with `db: ok` within a bounded retry window). Only a green gate finalizes;
a red gate restores the previous binary, restarts again, and exits nonzero. The
failure mode this defends against is the one you will actually hit: a
cross-compilation mistake or a migration that fails at startup, discovered not by a
customer but by the script that refused to leave the host broken. Note the ordering
subtlety — migrations run at startup, so the *old* binary must tolerate the *new*
schema, or rollback after a migration is itself a broken state. For this roadmap's
additive migrations that holds; the constraint is worth writing in the script's
header comment.

<Mermaid
  caption="The health gate is the decision point that makes a deploy transactional: every red path ends with the previous binary serving and a nonzero exit, so a broken build is discovered by the script, not by a customer."
  code={`
flowchart TD
    A["rsync artifact to quickspin.new"] --> B["mv current binary to quickspin.prev"]
    B --> C["mv quickspin.new into place<br/>(mv is atomic on one filesystem)"]
    C --> D["systemctl restart quickspin<br/>(migrations run at startup)"]
    D --> E{"gate: /v1/status shows the new<br/>version with db: ok within<br/>a bounded retry window?"}
    E -- "green" --> F["finalize — deploy done"]
    E -- "red" --> G["restore quickspin.prev<br/>and restart again"]
    G --> H["exit nonzero —<br/>old version still serving"]
`}
/>

<StudyPrompt question="Why cross-compile from the Mac instead of building on the server?">
  Two reasons compound. First, the artifact you tested is byte-identical to the one
  you shipped — building on the host inserts an untested compile between validation
  and production. Second, it keeps the server free of a Go toolchain, which is one
  less thing to patch and one less way the host becomes a snowflake. Go makes this
  nearly free: `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build` from any machine
  emits a static linux/amd64 binary. The arch flip (arm64 Mac to amd64 server) is
  exactly why `make release` must set both variables explicitly rather than trusting
  the defaults.
</StudyPrompt>

## Design and interfaces

```text
Host: Hetzner CX-class (x86_64), Ubuntu LTS. (/dev/kvm comes free — only the
  optional microVM track would ever use it.)
Provisioning: hack/cloud-init.yaml checked into the repo — creates a deploy user,
  installs Caddy + Docker, writes systemd units, ufw rules. The host must be
  reproducible from this file alone; no hand-configured snowflake.
Processes: quickspin serve (localhost:8080) <- Caddy (:443, TLS) ;
  quickspin-hostd registering with the plane over localhost, exactly the
  roadmap 13 path; docker-backed sandboxes managed through hostd as in Lima.
Lifecycle scripts: hack/prod-up.sh (create server from cloud-init, deploy, mint
  host token, health-gate) and hack/prod-down.sh (destroy the server; Neon state
  survives, so the next prod-up resumes the same tenant and history).
Secrets: /etc/quickspin/env (mode 600) — Neon URL, host token, nothing in the repo.
CLI: --base-url flag + QUICKSPIN_BASE_URL env var (default stays localhost).
New endpoint: GET /v1/status (unauthenticated) — version, uptime, host summary,
  DB ping.
```

Committed decisions: single host, no HA, no CDN, no container packaging (a static Go
binary makes Docker-for-deploy pure ceremony — Docker on the host is the sandbox
backend, not a deploy mechanism); deploys are brief-downtime restarts (seconds —
acceptable and stated); the Lima path keeps working with SQLite so local dev never
needs the internet; plane and hostd share the host and talk over localhost — the
process split and registration path are identical to Lima, only the address is real;
tear-down is first-class: the roadmap is done when the *cycle* works, not when a
server happens to be running. gVisor (roadmap 18) on this host is later a config
change to the same Docker backend — flipping the default runtime handler, reversible,
with snapshots still working.

<Callout tone="system" title="The host is reproducible from the repo or it doesn't count">
  Every package installed, unit written, and firewall rule added must originate from
  `hack/cloud-init.yaml`. If you fix something over SSH, backport it to the file and
  be prepared to prove it by rebuilding the server from scratch. A snowflake host is
  unrecoverable exactly when you need to recover it.
</Callout>

## Tasks

<TaskList>
  <Task title="Add make release for the linux/amd64 binary">
    `make release` cross-compiles the plane binary for linux/amd64 (note the arch
    flip from your arm64 Mac).
  </Task>
  <Task title="Write hack/cloud-init.yaml and create the server">
    Deploy user, Caddy + Docker install, systemd units for both the plane and
    hostd, ufw rules — then create the Hetzner server from that file via console
    or CLI. hostd registers with the plane over localhost at boot, the same
    roadmap 13 path Lima exercises daily.
  </Task>
  <Task title="DNS record and Caddyfile; confirm TLS">
    Point `api.quickspin.<yourdomain>` at the host's IP, write the Caddyfile reverse
    proxy, and verify a valid Let's Encrypt certificate is served.
  </Task>
  <Task title="Write hack/deploy.sh with a health-check gate and rollback">
    Rsync the artifact, restart the unit, poll `/v1/status`, and restore the previous
    binary automatically on a red gate.
  </Task>
  <Task title="Add /v1/status and the CLI base-URL option">
    The unauthenticated status endpoint on the plane, plus `--base-url` /
    `QUICKSPIN_BASE_URL` in the CLI with localhost as the default.
  </Task>
  <Task title="Mint a prod tenant and key">
    Over SSH with the admin CLI — the only manual step, and it produces a secret that
    lives on your Mac, not in the repo.
  </Task>
  <Task title="Write hack/validate-14.sh and accept prod by hand">
    The Mac-side validation script from the definition of done, then a hand-driven
    create → exec → files → destroy session against the public domain, recorded in
    the closing note.
  </Task>
  <Task title="Wrap the cycle in hack/prod-up.sh and hack/prod-down.sh">
    prod-up: create the server from cloud-init, deploy, mint the host token,
    health-gate. prod-down: destroy the server (Neon state survives). Done means
    running the full cycle twice — the second spin-up proves nothing depended on
    leftovers from the first.
  </Task>
</TaskList>

## Hint steps

<HintSteps>
  <Hint
    title="Get the amd64 binary building before touching any server"
    check="file bin/quickspin-linux-amd64 reports an x86-64 statically linked ELF."
  >
    Start where no cloud account is needed. `make release` is
    `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o bin/...` (CGO off keeps it
    static — check whether your SQLite driver needs cgo; the prod path uses Neon so
    the pure-Go build may be fine, but decide explicitly). `file` and `uname -m` are
    your truth commands throughout this roadmap.
  </Hint>
  <Hint
    title="Draft cloud-init locally, then create the server from it"
    check="hcloud server create --user-data-from-file hack/cloud-init.yaml boots a host where systemctl status caddy and ufw status show everything you declared, with zero SSH edits."
  >
    Read the cloud-init module docs for `users`, `packages`, `write_files`, and
    `runcmd` — that's the whole vocabulary you need. Write the systemd unit and ufw
    rules (`ufw default deny incoming`, `ufw allow 443/tcp`, `ufw allow OpenSSH`,
    `ufw --force enable`) as `write_files` + `runcmd` entries. Debug a bad boot with
    `cloud-init status --long` and `/var/log/cloud-init-output.log` on the host. Pick
    a CX line explicitly; `ls /dev/kvm` is worth one look while you're there — it
    comes free on Hetzner and is what keeps the optional microVM track (26) a
    software change rather than a re-provisioning, though nothing in the core arc
    uses it.
  </Hint>
  <Hint
    title="DNS then Caddy, in that order"
    check="curl https://api.quickspin.yourdomain/v1/status returns JSON over a certificate curl trusts, with no -k flag."
  >
    Caddy cannot pass the ACME challenge until the A record resolves to the host, so
    create the record first and confirm with `dig +short api.quickspin.yourdomain`.
    The Caddyfile is two lines: the site address and `reverse_proxy localhost:8080`.
    While DNS propagates you can pre-test the proxy path with
    `curl --resolve api.quickspin.yourdomain:443:<ip> ...`. If certificates fail,
    `journalctl -u caddy` names the exact ACME error.
  </Hint>
  <Hint
    title="Write the systemd unit as if crashes are routine"
    check="kill -9 the plane's PID over SSH; systemctl status shows a new PID within seconds and journalctl -u quickspin has both the death and the restart."
  >
    The unit needs `Restart=on-failure`, `After=network-online.target` (plus
    `Wants=`), `EnvironmentFile=/etc/quickspin/env`, and a non-root `User=`. Secrets
    live only in that env file at mode 600 — verify with `stat -c %a`. After editing
    units remember `systemctl daemon-reload`; forgetting it is the classic "my change
    did nothing" trap. `systemctl enable` is what makes the reboot check pass —
    without it the unit restarts on crash but not on boot.
  </Hint>
  <Hint
    title="Structure deploy.sh around the gate, not the copy"
    check="Deploying a real build flips /v1/status to the new version; deploying a binary you deliberately broke (e.g. exit 1 as main) leaves the old version serving and the script exits nonzero."
  >
    Shape: `set -euo pipefail`; rsync to `quickspin.new`; `mv` current to
    `quickspin.prev`; `mv` new into place (mv is atomic on one filesystem, cp is
    not); `systemctl restart`; then a retry loop of `curl` against `/v1/status`
    comparing the version field. The rollback branch reverses the two `mv`s and
    restarts again. Build the broken-binary test *now* — it is the deploy-safety
    check in the definition of done and the easiest time to write it is while the
    happy path is fresh.
  </Hint>
  <Hint
    title="Thread the base URL through the CLI without breaking local defaults"
    check="The CLI drives prod with only QUICKSPIN_BASE_URL and the prod API key exported, and still drives Lima with neither set."
  >
    Precedence should be explicit flag > env var > localhost default, resolved in
    one place (the client constructor), not scattered — the same precedence the SDKs
    will adopt in roadmaps 22–23, so pin it with a test now. Then mint the prod
    tenant over SSH with the admin CLI against the live DB.
  </Hint>
  <Hint
    title="Diagnosing the likely failure: a port that answers when it shouldn't"
    check="From the Mac, nc -vz -w3 <host-ip> 8080 and a guest port both fail (refused or timed out), while 443 connects; hack/validate-14.sh asserts exactly this."
  >
    If 8080 is reachable from the internet, check three suspects in order. One: the
    plane's listen address — `localhost:8080`, never `:8080`; confirm on the host
    with `ss -tlnp`. Two: ufw actually enabled (`ufw status verbose` says active) —
    cloud-init's `runcmd` may have needed `--force` to enable non-interactively.
    Three: Docker — the sandbox backend on this host inserts iptables rules that
    bypass ufw for published container ports, a notorious trap; avoid publishing
    ports or bind them to 127.0.0.1. Write the negative probe with a
    timeout (`nc -w3` or `curl --max-time`) so "filtered" counts as pass, not hang.
  </Hint>
</HintSteps>

## Definition of done

`hack/validate-14.sh` runs **from the Mac** against the public URL, exits 0, checking:

- `/v1/status` over valid TLS reports the expected version, `db: ok`, and one
  ready registered host — proof the roadmap 13 registration path, not a static
  config, wired this host in.
- Full lifecycle with `backend: docker`: create → running, exec streams, file
  round-trip, destroy — all via the public API.
- Save/restore works in prod: `save` a sandbox, create from the snapshot, and verify
  the restored filesystem (roadmap 12's docker-commit snapshots run natively on this
  backend).
- Auth is enforced (no key ⇒ 401) and the plane's plaintext port and a guest port are
  **not** reachable from the internet (probe and assert connection refused/filtered).
- Reboot resilience: `ssh reboot`, then within a bounded window `/v1/status` is green
  and a new sandbox can be created (systemd + reconciler did their jobs).
- Deploy safety: `hack/deploy.sh` with a deliberately broken binary rolls back and the
  API stays serving the old version.
- The cycle: `hack/prod-down.sh` destroys the server, and a second
  `hack/prod-up.sh` from nothing reaches a green `validate-12` — spin-up is a
  capability, not an artifact.

Plus: the manual acceptance session — create, exec, files, destroy driven from the
Mac against the public domain — recorded in the roadmap's closing note. The SDK-era
demo harness (roadmap 24) repeats this against prod with only env-var changes at the
end of the sequence.

Deliberately untested: sustained load, disk-full behavior, Hetzner outages, multiple
concurrent deploys.

## Solo-developer tradeoffs

No Kubernetes, no Terraform, no CI/CD pipeline, no blue-green: for one host, each of
those adds more failure modes than it removes, and cloud-init + a deploy script keeps
the whole production story readable in two files — itself an operator-legible judgment
call. The real risks accepted and written down: single point of failure (acceptable for
an initial single-host deployment), brief deploy downtime, and secrets managed by file
permissions rather than a vault.
What is *not* compromised: TLS-only exposure, a firewalled plane, hashed keys, and
rollback-capable deploys — the floor below which an initial deployment becomes a
liability.

## Go deeper

<Resources>
  <Resource
    kind="docs"
    title="systemd.service — service unit configuration"
    href="https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html"
  >
    The authoritative reference for every directive the unit file uses — `Restart=`,
    `ExecStart=`, `Type=` — including restart-rate limiting the roadmap doesn't cover.
  </Resource>
  <Resource
    kind="docs"
    title="Cloud-init module reference"
    href="https://cloudinit.readthedocs.io/en/latest/reference/modules.html"
  >
    The full vocabulary behind `users`, `packages`, `write_files`, and `runcmd`, with
    the per-module options the hint steps only gesture at.
  </Resource>
  <Resource
    kind="docs"
    title="Caddy: Automatic HTTPS"
    href="https://caddyserver.com/docs/automatic-https"
  >
    Exactly what Caddy does on your behalf — certificate issuance, renewal, OCSP
    stapling, HTTP→HTTPS redirects — so the two-line Caddyfile stops being magic.
  </Resource>
  <Resource
    kind="article"
    title="How It Works"
    href="https://letsencrypt.org/how-it-works/"
    by="Let's Encrypt"
  >
    The ACME domain-validation protocol underneath Caddy's automation — the challenge
    flow that explains why the A record must resolve before certificates can issue.
  </Resource>
  <Resource
    kind="article"
    title="The Twelve-Factor App: Config"
    href="https://12factor.net/config"
    by="Adam Wiggins"
  >
    The original statement of the "same binary, different env" rule this roadmap applies —
    and the test for whether config has leaked into code.
  </Resource>
</Resources>
