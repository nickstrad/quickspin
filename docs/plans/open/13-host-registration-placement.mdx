# 13 — Host registration, heartbeats, and placement

Depends on: 08, 09 (07's guest architecture assumed). Runs *before* 14: prod
arrives afterward as an on-demand cloud spin-up of the fleet machinery built here.

## Summary

Turn roadmap 08's single statically-configured host agent into a fleet, in two halves.
First, **scheduling**: a second statically-configured Lima hostd joins the first, the
plane grows a `hosts` table and a pure placement function, and the capacity
reservation is made race-safe — all the multi-host logic, none of the liveness
machinery. Second, **registration**: hosts become *discoverable and mortal* — hostd
registers itself with the control plane at boot and heartbeats every few seconds,
the reconciler declares hosts dead when heartbeats stop and fails their sandboxes,
and the static host config from the first half is deleted. It ends with an n-host
Lima fleet surviving the failure-injection suite; putting the same machinery on a
cloud host is roadmap 14's job.

It also delivers `hack/failure-suite.sh`: a named failure-injection suite that
deliberately kills a hostd mid-create, silences heartbeats without killing anything,
replays API requests, exhausts capacity, and abandons runtime resources — turning
every distributed-systems claim this roadmap makes into a scenario you have personally
run, not a textbook answer.

## Industry context

This is the canonical control-plane/data-plane split: Kubernetes' apiserver/kubelet,
Nomad's server/client, and (per their engineering posts) how E2B and Fly place microVMs
on host fleets. The mechanisms you're building are the textbook trio — registration,
heartbeats with a dead-host threshold, and capacity-aware placement — plus the rule
that makes distributed state survivable: the database's desired state is authoritative,
hosts hold no durable truth, and a dead host's sandboxes are *failed, not
migrated* (sandboxes are stateful; honest platforms kill them and let clients recreate
from snapshots — exactly why roadmap 12 exists).

## Key concepts

- Growing roadmap 08's internal API: registration and heartbeat endpoints, and its
  single shared token replaced by per-host revocable tokens minted by the plane's
  admin CLI (mTLS still the industry answer, still deferred).
- Failure detection theory made concrete: heartbeat interval vs death threshold,
  why "no heartbeat" means *unknown* not *dead*, and what a flapping host does to
  naive logic.
- Placement as a pure function: `place(spec, []Host) (hostID, error)` — trivially
  testable, no I/O (the code-structure reference doc's pattern applied for real). But
  the pure function only *chooses*; the *reservation* is a check-then-act hazard — two
  concurrent creates can both pick a host's last slot. Committing the placement re-checks
  capacity inside the insert's transaction (`FOR UPDATE` on the host row), the same
  fold-the-check-into-the-reserve rule as roadmap 11's quota, one layer down.
- The process-boundary error rules from roadmap 08 extend to the new surface:
  registration and heartbeat failures mint typed errors in the plane's host client,
  never fake wraps of errors this process never had.
- The reconciler generalized across two layers: converging sandboxes *and* hosts.

### Concept: a missed heartbeat is absence of evidence, not evidence of death

A heartbeat is the only signal the plane has about a host, and it is a weak one: when
heartbeats stop, the plane cannot distinguish a crashed hostd from a network
partition, a GC pause, or a plane-side ingestion hiccup. The host's sandboxes may be
running perfectly. That ambiguity is why the design uses *two* thresholds rather than
one. At 30 seconds (three missed 10-second beats) the host becomes `unready`: the
plane stops sending new work — a cheap, reversible act, because refusing placement on
a healthy host costs a little capacity while placing onto a dead one loses a
customer's sandbox. Only at 2 minutes does the plane commit to `dead` and take the
expensive, effectively irreversible action of failing the host's sandboxes and
emitting events. The gap between the thresholds is the flapping budget: a host whose
network blips for 40 seconds goes unready, resumes heartbeating, and returns to
`ready` with nothing killed. Naive single-threshold logic turns every blip into a
mass sandbox failure — `TestFlappingHostRecovers` exists to pin the correct behavior.

<Mermaid
  caption="The host state machine the reconciler drives from heartbeat age alone. Every transition into unready is cheap and reversible; the single transition into dead is the only one that destroys anything, which is why it gets the long threshold."
  code={`
stateDiagram-v2
    [*] --> ready: POST /register at boot
    ready --> unready: no heartbeat for 30s — placement stops
    unready --> ready: heartbeat resumes (the flapping budget)
    unready --> dead: silence reaches 2m
    dead --> ready: hostd restarts and re-registers
    ready --> draining: admin host drain
    note right of dead : sandboxes marked failed, events emitted — never migrated
    note right of draining : no new placements, running sandboxes finish or expire
`}
/>

<Figure caption="Hosts push registration and heartbeats up; the plane pushes Runtime calls down to a placed host; the reconciler watches heartbeat age and demotes silent hosts.">
  <svg viewBox="0 0 620 268" role="img" aria-label="Control plane and registered host agents with registration, heartbeats, and placement">
    <defs>
      <marker id="arrow17" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#cf5436" />
      </marker>
      <marker id="arrow17g" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#caa552" />
      </marker>
    </defs>
    <rect x="20" y="20" width="250" height="140" fill="none" stroke="#18231f" strokeWidth="1.5" />
    <text x="145" y="40" textAnchor="middle" fontSize="12" fill="#18231f">control plane (no /dev/kvm)</text>
    <rect x="40" y="54" width="95" height="36" fill="none" stroke="#405049" strokeWidth="1" />
    <text x="87" y="76" textAnchor="middle" fontSize="10" fill="#405049">hosts table</text>
    <rect x="155" y="54" width="95" height="36" fill="none" stroke="#405049" strokeWidth="1" />
    <text x="202" y="76" textAnchor="middle" fontSize="10" fill="#405049">placement</text>
    <rect x="40" y="106" width="210" height="36" fill="none" stroke="#405049" strokeWidth="1" />
    <text x="145" y="128" textAnchor="middle" fontSize="10" fill="#405049">reconciler: heartbeat age → unready → dead</text>
    <rect x="380" y="20" width="220" height="76" fill="none" stroke="#18231f" strokeWidth="1.5" />
    <text x="490" y="40" textAnchor="middle" fontSize="12" fill="#18231f">quickspin-hostd (host A)</text>
    <text x="490" y="58" textAnchor="middle" fontSize="10" fill="#405049">Runtime over HTTP + bearer token</text>
    <text x="490" y="74" textAnchor="middle" fontSize="10" fill="#405049">sandboxes as local processes</text>
    <rect x="380" y="120" width="220" height="56" fill="none" stroke="#18231f" strokeWidth="1.5" strokeDasharray="6 4" />
    <text x="490" y="144" textAnchor="middle" fontSize="12" fill="#405049">hostd (host B) — silent</text>
    <text x="490" y="162" textAnchor="middle" fontSize="10" fill="#405049">last_heartbeat_at aging out</text>
    <line x1="380" y1="40" x2="272" y2="52" stroke="#caa552" strokeWidth="1.5" markerEnd="url(#arrow17g)" />
    <text x="330" y="34" textAnchor="middle" fontSize="10" fill="#7f5d43">POST /register at boot</text>
    <line x1="380" y1="72" x2="272" y2="84" stroke="#caa552" strokeWidth="1.5" strokeDasharray="5 4" markerEnd="url(#arrow17g)" />
    <text x="330" y="92" textAnchor="middle" fontSize="10" fill="#7f5d43">POST /heartbeat / 10s</text>
    <line x1="252" y1="60" x2="378" y2="52" stroke="#cf5436" strokeWidth="1.5" markerEnd="url(#arrow17)" />
    <text x="316" y="52" textAnchor="middle" fontSize="10" fill="#8f3423">create/destroy →</text>
    <line x1="250" y1="128" x2="378" y2="142" stroke="#cf5436" strokeWidth="1.5" strokeDasharray="5 4" markerEnd="url(#arrow17)" />
    <text x="312" y="128" textAnchor="middle" fontSize="10" fill="#8f3423">no beat &gt; 2m: dead,</text>
    <text x="312" y="142" textAnchor="middle" fontSize="10" fill="#8f3423">sandboxes → failed</text>
    <rect x="20" y="200" width="580" height="52" fill="none" stroke="#405049" strokeWidth="1" strokeDasharray="6 4" />
    <text x="310" y="220" textAnchor="middle" fontSize="10" fill="#405049">DB desired state is the only durable truth — hosts can be lost and rebuilt from it</text>
    <text x="310" y="238" textAnchor="middle" fontSize="10" fill="#405049">workload ops (exec, files) still flow plane → hostd → guest, unchanged from roadmap 08</text>
  </svg>
</Figure>

### Concept: choosing a host is pure; claiming a slot is not

`place(spec, []Host)` is deliberately a function with no I/O: filter to hosts that are
`ready`, match the requested backend and arch, have headroom, then pick the
least-loaded. Every placement policy question becomes a table test. But the function
runs on a snapshot of the hosts table, and between "I read that host A has one slot
left" and "I inserted a sandbox on host A", another request can do the same read.
Both pass the pure check; the host ends up over capacity. This is the classic
check-then-act race, and the fix is the same one roadmap 11 used for quotas: fold the
check into the reservation by re-verifying capacity *inside* the transaction that
inserts the sandbox row, with the host row locked (`SELECT ... FOR UPDATE`). The pure
function remains the policy; the transaction is the arbiter.
`TestConcurrentPlacementNeverOverfillsHost` must fail if the transactional re-check
is deleted — if it still passes without it, the test is not exercising the race.

<Mermaid
  caption="Two creates race for host A's last slot. Both pure place() calls pick A from the same stale snapshot — that is allowed — because the FOR UPDATE re-check serializes them at the row write, where the second one discovers the slot is gone."
  code={`
sequenceDiagram
    participant R1 as create request 1
    participant R2 as create request 2
    participant DB as Postgres
    R1->>DB: read hosts snapshot
    R2->>DB: read hosts snapshot
    Note over R1,R2: both place() calls choose host A's last slot
    R1->>DB: BEGIN, SELECT host A FOR UPDATE
    R1->>DB: recount active sandboxes: slot free
    R1->>DB: INSERT sandbox, COMMIT
    R2->>DB: BEGIN, SELECT host A FOR UPDATE
    Note over R2,DB: blocks until R1 commits
    R2->>DB: recount active sandboxes: slot gone
    R2-->>R2: abort with typed no_capacity
`}
/>

<KeyPoint label="Invariant">
  The pure placement function may be wrong (stale snapshot); the reservation
  transaction may not be. Capacity is enforced where the row is written, never where
  it is read.
</KeyPoint>

### Concept: hosts hold no durable truth

The split only works because of a state-ownership rule: the database's desired state
is authoritative, and hostd is a stateless executor over its local runtime. hostd
persists nothing it cannot rebuild — on boot it re-registers, reports what is actually
running (discovered from labels, roadmap 02's rediscovery mechanism paying off across a
process boundary), and takes orders. That is why `kill -9` on hostd is a legitimate
test rather than a catastrophe, and why a dead host's sandboxes are *failed, not
migrated*: the sandbox's memory and disk lived only on that host, so pretending to
move it would be lying to the client. Failing fast with an event, and letting the
client recreate from a roadmap 12 snapshot, is what honest platforms do. The same rule
explains `no_capacity` as a fast typed error rather than a queue: queueing is durable
plane-side state with its own fairness and expiry semantics — real future work, not a
default to back into.

<StudyPrompt question="Why does the plane never SSH into a host?">
  SSH is a human's escape hatch, not a program's API: unversioned, untyped,
  screen-scraped, and offering root. The moment the plane depends on SSH, host
  behavior can no longer be faked in a unit test, auth can't be scoped to specific
  verbs, and every operation has a hidden dependency on shell details of the host
  image. Two typed HTTP surfaces (plane API and hostd API) keep every cross-process
  interaction testable with a fake client and revocable with a token — which is
  exactly what `TestDeadHostFailsItsSandboxes` and friends rely on.
</StudyPrompt>

## Design and interfaces

```text
quickspin-hostd (per host — the binary exists since roadmap 08; this roadmap adds):
  POST /register     at boot: host name, arch, backend(s), capacity (max sandboxes)
  POST /heartbeat    every 10s to the plane: running count, disk/mem headroom
  (the Runtime surface and guest passthrough are unchanged from roadmap 08)

Control plane:
  hosts(id, addr, arch, backend, capacity, state{ready,unready,draining,dead},
        last_heartbeat_at, registered_at)
  Placement: filter ready+backend+capacity, pick least-loaded; no host ⇒ create
  fails fast with a typed "no_capacity" error (backpressure, not queueing — queueing
  is future work and saying so is the design decision).
  Reconciler additions: no heartbeat > 30s ⇒ unready (no new placements);
  > 2m ⇒ dead ⇒ its sandboxes -> failed, events emitted.
  Admin: quickspin admin host list|drain|token ; GET /v1/status gains host summary.
  sandboxes gains host_id.
```

Committed decisions: the plane never SSHes to hosts — everything flows through the
two HTTP surfaces; roadmap 08's single shared token is replaced by per-host revocable
tokens; the plane's static host config survives only through the first half — the
scheduling work statically seeds two Lima hostds into the hosts table, and that
seeding is deliberately throwaway, deleted when registration lands so the hosts
table's registered rows are the only source of hosts (leaving both paths means
registration bugs hide behind the fallback); `drain` stops placement but leaves
running sandboxes to finish or expire (the graceful-maintenance primitive roadmap 15
automates); local dev keeps roadmap 08's plane + hostd pair, now connected through
registration instead of a static address, so the fleet path is exercised daily.

<Callout tone="warning" title="Thresholds are compared against the DB clock you wrote, not wall time you remember">
  Every heartbeat handler writes `last_heartbeat_at`; every reconciler pass computes
  `now - last_heartbeat_at`. Both must use the same injected clock in tests, or the
  threshold tests become flaky sleeps. Design the state-transition logic as a pure
  function of `(host, now)` from the start — the fake-clock tests in the definition
  of done assume it.
</Callout>

## Tasks

<TaskList>
  <Task title="Add the hosts table and statically seed a second Lima hostd">
    The `hosts` schema and migration, `host_id` on sandboxes, and config that seeds
    two Lima hostd rows into the table at boot. The seeding is accepted throwaway:
    the registration task deletes it.
  </Task>
  <Task title="Implement placement and wire it into async create">
    The pure `place` function, the transactional capacity re-check at insert, and the
    typed `no_capacity` error surfaced through the API (the SDKs pick it up in
    roadmaps 22–23). Consecutive creates land on both statically-seeded hostds.
  </Task>
  <Task title="Add registration and heartbeats; delete the static seeding">
    `POST /register` and `POST /heartbeat` on the plane, hostd's boot-time
    registration call and ticker loop, roadmap 08's shared token replaced by per-host
    revocable tokens — and the static seeding removed in the same change, so
    registered rows are the only source of hosts. A third (and nth) hostd now joins
    by being started, not by being configured.
  </Task>
  <Task title="Extend the reconciler to host state">
    Unready and dead transitions from heartbeat age, failing a dead host's sandboxes,
    and host events.
  </Task>
  <Task title="Add admin verbs and the status host summary">
    `quickspin admin host list|drain|token`, and a host summary in `GET /v1/status`.
  </Task>
  <Task title="Write hack/failure-suite.sh — the failure-injection suite">
    Five deliberate injections against a real plane + hostd in Lima, each with an
    asserted recovery: kill -9 hostd mid-create; SIGSTOP hostd so heartbeats
    stop while sandboxes keep running (unready, never dead, recovers on SIGCONT);
    replay a create with the same idempotency key during and after placement; fill
    a host to capacity and assert typed no_capacity, then admission after a destroy
    frees a slot; plant an orphan container bearing the quickspin label and assert
    the sweep collects it.
  </Task>
</TaskList>

## Hint steps

<HintSteps>
  <Hint
    title="Seed two hostds statically — the hosts table before liveness"
    check="Two Lima hostds serve side by side, the hosts table shows both rows at boot, and sandboxes still work end-to-end on each."
  >
    Start the second hostd beside the first (distinct ports and per-hostd label
    namespaces so their orphan sweeps don't collect each other's containers), and
    seed both into the new hosts table from config at plane boot. No registration,
    no heartbeats yet — the rows are just data for placement to read. This half is
    deliberately throwaway wiring; the point is that every scheduling test that
    follows runs against real multi-host state.
  </Hint>
  <Hint
    title="Write place as a pure function and table-test it before any wiring"
    check="TestPlacementPrefersLeastLoaded, TestPlacementFiltersArchAndBackend, and TestPlacementNoCapacityIsTypedError pass with no database or HTTP in sight."
  >
    Signature per the design: `place(spec, []Host) (hostID, error)`. Filter (ready,
    backend, arch, headroom) then select least-loaded, with a deterministic tie-break
    (lowest ID) so tests don't flake. The empty-result case returns the typed
    `no_capacity` error, constructed here so the API layer only translates it.
    Getting these tables green first means the later wiring bugs can only be in
    wiring.
  </Hint>
  <Hint
    title="Make the reservation transactional, then try to break it"
    check="TestConcurrentPlacementNeverOverfillsHost admits exactly M of N creates — and starts failing when you comment out the FOR UPDATE re-check."
  >
    Inside the create transaction: `SELECT ... FROM hosts WHERE id = $1 FOR UPDATE`,
    recount that host's active sandboxes, and abort with `no_capacity` if the slot
    vanished between the pure choice and now. The concurrency test is N goroutines
    calling create against one host of capacity M through the real store (this is
    what roadmap 09's Postgres is for), asserting exactly M successes and N−M typed
    failures. Run it with `go test -race -count=10` — a race that shows up one run
    in five is still a race. Then do the deletion experiment in the check: a
    concurrency test that cannot fail proves nothing.
  </Hint>
  <Hint
    title="Registration replaces the seeding — delete the static config"
    check="The plane boots with no hosts configured, both hostds register at boot, a third hostd joins by simply starting, and a create lands on a registered host."
  >
    Now make the rows self-reporting: hostd gains a startup call to `POST /register`
    and a `time.Ticker` heartbeat loop; its token moves from roadmap 08's shared
    value to a per-host one checked by the same middleware. Delete the static
    seeding in the same change so the registered rows are the only path — leaving
    both means registration bugs hide behind the fallback. The plane-side handlers
    are small: an upsert and a single-row UPDATE (next hint).
  </Hint>
  <Hint
    title="Registration and heartbeat are upserts keyed on host identity"
    check="Starting hostd twice produces one hosts row, and last_heartbeat_at visibly advances every ~10s while it runs."
  >
    Decide the host's stable identity (its name is fine at this scale) and make
    `/register` an upsert (`INSERT ... ON CONFLICT ... DO UPDATE`) so re-registration
    after a crash refreshes `addr`, capacity, and state rather than duplicating the
    row — a hostd restart must be routine. The heartbeat loop in hostd is a
    `time.Ticker` posting the running count and headroom; on the plane it is a
    single-row UPDATE of `last_heartbeat_at` plus the reported gauges. Registration
    should also flip an unready host straight back to ready.
  </Hint>
  <Hint
    title="Drive host state transitions from an injected clock"
    check="TestMissedHeartbeatsMarkUnreadyThenDead passes by advancing a fake clock past 30s and 2m — no time.Sleep anywhere in the test."
  >
    Give the reconciler a `now func() time.Time` (or a small Clock interface) instead
    of calling `time.Now` inline, and factor the decision into a pure function of
    `(host, now)` returning the target state. Then the threshold tests are tables:
    beat at t=0, advance to t=31s, expect unready; advance to t=2m1s, expect dead
    plus the sandbox-failing side effects via a fake host client.
    `TestFlappingHostRecovers` is the same machinery with a heartbeat landing at
    t=90s — assert ready again and, critically, that no destroy call ever reached
    the fake client.
  </Hint>
  <Hint
    title="Dead-host handling is a state change plus events, never a host call"
    check="TestDeadHostFailsItsSandboxes marks only that host's sandboxes failed, emits an event per sandbox, and the fake host client records zero calls to the dead host."
  >
    When a host crosses the dead threshold: update its sandboxes to `failed`, emit
    events, done. Do not try to destroy through the dead hostd — it is unreachable
    by definition, and the runtime-level orphan sweep (roadmap 06, now running inside
    hostd) collects the corpses if the host ever returns. `drain` is the gentler
    sibling: a state that placement filters out while everything running continues —
    `TestDrainStopsPlacementKeepsRunning` pins both halves.
  </Hint>
  <Hint
    title="Diagnosing the likely failure: the kill -9 scenario misbehaves"
    check="In hack/validate-13.sh, after kill -9 the host reaches dead within the thresholds, its sandbox is failed with an event, creates fail fast with no_capacity — and a hostd restart re-registers and garbage-collects the orphan container."
  >
    Three distinct bugs hide in this one scenario. If the host never goes dead: the
    reconciler is comparing against a stale `now` or the interval between reconciler
    passes exceeds the threshold window — log heartbeat age each pass and watch it
    grow. If creates hang instead of failing fast: the plane's host client lacks an
    `http.Client` timeout and is waiting on a socket that will never answer — every
    cross-process client needs a deadline. If the orphan container survives the
    hostd restart: the sweep is filtering by a label the plane-era create wrote but
    the hostd-era create doesn't (or vice versa) — `docker ps -a --filter
    label=quickspin.managed=true` inside Lima shows the truth. Use generous
    test-config thresholds (say 3s/10s via env) so the script runs in seconds, but
    keep the 10s/30s/2m production values as the documented defaults.
  </Hint>
</HintSteps>

## Definition of done

Unit tests (`make test`, fake clock, fake host client):

- `TestPlacementPrefersLeastLoaded`, `TestPlacementFiltersArchAndBackend`,
  `TestPlacementNoCapacityIsTypedError` (pure-function table tests).
- `TestMissedHeartbeatsMarkUnreadyThenDead` — clock-driven, both thresholds.
- `TestDeadHostFailsItsSandboxes` — with events, and no effect on other hosts'
  sandboxes.
- `TestFlappingHostRecovers` — heartbeat resumes before the dead threshold ⇒ ready
  again, nothing was killed.
- `TestDrainStopsPlacementKeepsRunning`.
- `TestConcurrentPlacementNeverOverfillsHost` — N concurrent creates against one
  host with capacity M admit exactly M (the placement twin of roadmap 11's quota-race
  test; must fail if the transactional re-check is removed).

Integration `hack/validate-13.sh` (Lima, real plane + real hostds):

- Boot the plane with no hosts configured; two hostds register; consecutive creates
  spread across both and work end-to-end.
- A third hostd started mid-run joins the fleet with no config change and receives
  placements — the "n hosts by starting a process" property.
- `kill -9` one hostd: within the thresholds the host goes dead, its sandbox is
  `failed` with an event, and placement continues on the surviving hosts (creates
  fail fast with `no_capacity` only once the survivors are full).
- Restart the killed hostd: re-registers, placement resumes; the killed sandbox's
  container is garbage-collected (the runtime-level orphan sweep from roadmap 06
  still works through the host agent).

Failure-injection suite `hack/failure-suite.sh` (Lima, real plane + hostd) runs
green end-to-end, covering all five injections:

- hostd killed mid-create: the sandbox converges to `failed` (or `running` if the
  create had completed on the host side) with an event — never stuck in `pending`.
- Heartbeats silenced via SIGSTOP: host reaches `unready` and placement stops, but
  nothing is killed; SIGCONT within the dead threshold returns it to `ready`
  (the live twin of `TestFlappingHostRecovers`).
- Idempotency under retry: replaying a create with the same key during and after
  placement yields exactly one sandbox and one placement.
- Capacity exhaustion: creates beyond capacity fail fast with typed `no_capacity`;
  destroying one sandbox makes the next create admit.
- Abandoned resources: a hand-planted labeled container is collected by the sweep.

Deliberately untested: two hostds racing one sandbox ID (single-owner-by-placement
is asserted by design, not by lease — recorded as the known gap vs the reference
doc's ownership-lease stage), mTLS, placement under thousands of hosts.

## Solo-developer tradeoffs

Nomad and Kubernetes solve this with raft-replicated servers, ownership leases, and
work queues; you run one plane instance (Neon is the only HA piece) and skip leases
because single-plane placement can't double-assign. That is a real correctness
dependency on "exactly one control plane" — written down, and the first thing to fix
if this ever became multi-plane. Failing dead hosts' sandboxes instead of migrating
them is not a shortcut, though: it is what the big platforms do, and snapshots are
the recovery story.

## Go deeper

<Resources>
  <Resource
    kind="book"
    title="Designing Data-Intensive Applications"
    by="Martin Kleppmann, ch. 8 (The Trouble with Distributed Systems)"
  >
    The definitive treatment of why "no heartbeat" is ambiguity, not death — partial
    failures, unbounded delays, and process pauses, each of which can silence a
    perfectly healthy hostd.
  </Resource>
  <Resource
    kind="paper"
    title="Distributed Systems lecture notes"
    href="https://www.cl.cam.ac.uk/teaching/2122/ConcDisSys/dist-sys-notes.pdf"
    by="Martin Kleppmann, University of Cambridge"
  >
    A free, rigorous companion to DDIA: failure detectors, timeout choice, and clock
    behavior in ~90 pages — the theory behind the two-threshold design.
  </Resource>
  <Resource
    kind="article"
    title="Fallacies of distributed computing"
    href="https://en.wikipedia.org/wiki/Fallacies_of_distributed_computing"
    by="Peter Deutsch et al."
  >
    The classic checklist of assumptions ("the network is reliable", "latency is
    zero") that heartbeat and placement code silently makes until each one bites.
  </Resource>
  <Resource
    kind="docs"
    title="Kubernetes: Nodes"
    href="https://kubernetes.io/docs/concepts/architecture/nodes/"
  >
    The industrial-scale version of this roadmap's trio — node registration, dual
    heartbeats via NodeStatus and Lease objects, and condition-based eviction —
    worth comparing threshold by threshold.
  </Resource>
  <Resource
    kind="article"
    title="Addressing Cascading Failures"
    href="https://sre.google/sre-book/addressing-cascading-failures/"
    by="Google SRE Book"
  >
    What happens after a host dies at fleet scale: retries, load spirals, and why
    fast typed rejection (`no_capacity`) beats queueing as a default backpressure
    story.
  </Resource>
</Resources>
